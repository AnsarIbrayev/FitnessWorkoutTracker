# QA Log

Goal: document issues found during testing and the fixes applied.

## Issues & Fixes
1) Issue: (example) App shows blank screen when data is empty
   Fix: Added Empty State UI and safe null-handling

2) Issue:
   Fix:

3) Issue:
   Fix:

4) Issue:
   Fix:

5) Issue:
   Fix:
